<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- AppWidget container style -->
    <style name="Widget.LibrePods.AppWidget.Container" parent="Theme.Material3.DayNight">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <!-- AppWidget inner view style -->
    <style name="Widget.LibrePods.AppWidget.InnerView" parent="Theme.Material3.DayNight">
        <item name="android:background">@android:color/transparent</item>
    </style>

</resources>
